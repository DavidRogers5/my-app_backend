Project is a Node.js Babel framework API server. It make CRUD calls to a MS SQL Server backend for simple lists. User passes in an ID and list name. Application is now being moved via CI/CD deployment to the Azure Cloud.
